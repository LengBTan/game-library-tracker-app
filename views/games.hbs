<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>game list</title>
</head>
<body>
    <div>
        <a href="/logout">log out</a>
    </div>
    <div>
        {{#each gameEntry}}
            <p><a href="game/{{this.gameid}}">{{this.gameid}} {{this.title}}</a></p>
        {{/each}}
    </div>

</body>
</html>